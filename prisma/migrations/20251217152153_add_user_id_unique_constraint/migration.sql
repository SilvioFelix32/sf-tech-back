-- AddUniqueConstraint
-- Note: user_id is already PRIMARY KEY, which automatically enforces uniqueness
-- No database changes are needed as PRIMARY KEY already creates a unique index
-- This migration documents the @unique annotation added to the Prisma schema

